#magic ball

import random   #import random package

messages = ['It is certain', 'It is decidedly so', 'i am batman', 'modi modi modi', 'ha mai pappu hu', 'chai pi lo']

print (messages[random.randint(0,len(messages) -1)])    #print value in list 'messages' takes one random index and prints its value